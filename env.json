{
  "MONGODB_URI": {
    "description": "The Mongo DB connection string to the interview evaluation sandbox DB",
    "required": true,
    "default": null,
    "type": "string"
  },
  "PARTICIPANT": {
    "description": "Identifies the DB partition to use",
    "required": true,
    "default": null,
    "type": "string"
  }
}
